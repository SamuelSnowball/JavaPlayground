# Database Configuration, use these if want to setup a connection to a different DB
#db.driver=org.h2.Driver
#db.url=jdbc:h2:~/jooq
#db.username=sa
#db.password=
# Probably unnecessary as we aren't including values from application.properties, likely re-defining defaults here
logging.level.org.springframework.web=INFO
logging.level.root=INFO